import styled from "styled-components";

const Card = styled.div`
        box-shadow: 0 4px 8px 0 rgba(0,0,0,8);
        width: 94%;
        margin-top: 20px;
        position: relative;
        &:hover{
            
        }
`
export default Card;